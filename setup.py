from setuptools import setup

setup(
   name='RL_Implementation',
   version='0.1',
   description='Implement basic RL algorithms',
   author='KukJin Kim',
   author_email='ye20013@gmail.com',
   packages=['Environments'],  # would be the same as name
   #install_requires=['wheel', 'bar', 'greek'], #external packages acting as dependencies
)